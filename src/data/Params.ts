import * as datGui from "dat.gui";

/**
 * Global parameters
 */
export class Params {

    static Phaser = {
        gameWidth: 0
    };

    // modes
    static isDebugMode = false;

    // utils
    static datGui: datGui.GUI;

};
